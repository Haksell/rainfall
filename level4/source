#include <stdio.h>
#include <stdlib.h>

void    p(char *buffer)
{
    printf(buffer);
}

int n(void)
{
    char    buffer[512];

    fgets(buffer, 512, stdin);
    p(buffer);
    if (*((int *) 0x08049810) != 0x01025544)
        return (*((int *) 0x08049810));
    return (system("/bin/cat /home/user/level5/.pass"));
}

int main(void)
{
    return (n());
}
